export interface UserData {
  bio: string;
  createdAt: string;
  deletedAt: string;
  email: string;
  googleId: string;
  id: number;
  name: string;
  status: number;
  token: string;
  updatedAt: string;
}
